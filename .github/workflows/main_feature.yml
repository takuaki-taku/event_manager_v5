name: Deploy to EC2

on:
  push:
    branches:
      - feature/new-directory-type

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2 # バージョンを正しく修正
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.9"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Start Docker Compose
        run: |
          docker compose -f docker-compose.dev.yml up -d
      - name: Check Container Status
        run: |
          docker pss
          docker ps -a --filter name=event_manager_v5-db-1
          docker logs event_manager_v5-db-1
          docker inspect --format='{{json .State.Health}}' event_manager_v5-db-1
      - name: Wait for PostgreSQL
        run: |
          sleep 10
          CONTAINER_ID=$(docker ps -q --filter name=event_manager_v5-db-1)
          while [ -z "$CONTAINER_ID" ]; do
            echo "PostgreSQL container not found, waiting..."
            sleep 5
            CONTAINER_ID=$(docker ps -q --filter name=event_manager_v5-db-1)
          done
          docker exec "$CONTAINER_ID" pg_isready -U postgres
      - name: Run tests
        env:
          TEST_DATABASE_URL: postgresql://postgres:test_password@localhost:5432/test_db
        run: |
          pytest
      - name: Stop Docker Compose
        if: always()
        run: |
          docker compose -f docker-compose.dev.yml down
